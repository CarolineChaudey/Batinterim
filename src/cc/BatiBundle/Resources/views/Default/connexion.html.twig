
{% extends "ccBatiBundle:Default:index.html.twig" %}

{% block title %}
    BatiInterim
{% endblock %}

{% block body %}
    
    <h2>Connectez-vous à BatiInterim :</h2>
    
    {% for flash_message in app.session.flashbag.get('error') %}
        {{ flash_message }}
    {% endfor %}
    
    {{ form_start(form) }}
    
        {{ form_errors(form) }}
        
        {{ form_errors(form.login) }}
        {{ form_label(form.login) }}
        {{ form_widget(form.login) }}
        
        {{ form_errors(form.mdp) }}
        {{ form_label(form.mdp) }}
        {{ form_widget(form.mdp) }}
        
        {{ form_rest(form) }}
    
    {{ form_end(form) }}
    
{% endblock %}
